// import axios from 'axios';

import { type UserCreateData } from '../create-user-form-config/schema';

export const createUser = (data: UserCreateData) => {
  return '';
//   return (await axios.get(`/api/auth/permissions?email=${email}`)).data;
};
